[package]
name = "forge"
version = "0.1.0"
edition = "2021"
description = "Linguagem de script para game engine, compilada via LLVM (nome provisorio)"

[dependencies]
inkwell = { version = "0.9", features = ["llvm22-1"], default-features = false }
